


# 对外端口（宝塔 Nginx 占用 80，用 1145 或其他端口）
PORT=1145

# JWT 密钥（生成：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"）
JWT_SECRET=请替换为随机长字符串

# 首次启动设为 true 写入种子数据，完成后改回 false
RUN_SEED=true

# CORS 来源（同域部署无需修改）
CORS_ORIGIN=http://localhost
